{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TransCenderDatasetManifest (P3/P13/P14)",
  "description": "DRM-clean provenance gate. Every training row must pin source bytes (sha256), a license from the allow-list, and drm=false. Anything else is REJECTED by the ingest gate (see tests/test_data_trust.cpp).",
  "type": "object",
  "required": ["dataset", "commit", "rows"],
  "properties": {
    "dataset": { "type": "string", "minLength": 1 },
    "commit": { "type": "string", "pattern": "^[0-9a-fA-F]{40}$" },
    "local_only": { "type": "boolean", "description": "P13: self-hosted doctrine — true means no external API touched" },
    "baa_claim": { "type": "string", "description": "P13: must be NOT-CLAIMED unless a real audit exists. Fake BAA/SOC2 is forbidden." },
    "rows": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["source", "sha256", "license", "drm"],
        "properties": {
          "source": { "type": "string", "minLength": 1 },
          "sha256": { "type": "string", "minLength": 8 },
          "license": {
            "type": "string",
            "enum": ["CC0-1.0", "CC-BY-4.0", "CC-BY-SA-4.0", "MIT", "Apache-2.0", "BSD-2-Clause", "BSD-3-Clause", "Public-Domain", "Synthetic-Self-Owned"]
          },
          "drm": { "type": "boolean" },
          "notes": { "type": "string" }
        }
      }
    }
  }
}
