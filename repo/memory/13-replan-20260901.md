# 13 — RE-PLAN DELTA (Phase 0 → Phase 1, 2026-09-01)

**Date:** 2026-09-01
**Base plan:** 25-PHASE MERGED EDITION (TRANSCENDER MASTER PLAN) from skill input + `repo/memory/05-transcender-mission.md` + `research/audit_full_20260825.md`
**Re-context:** 6 parallel agents A1-A6 fresh full re-read (see `repo/sessions/phase0/` + `repo/memory/12-file-inventory.md`)
**Decision register locked:** D1-D9 from master plan §0.1 (Hybrid cender, .quant compat bidirectional, 5-pillar CLI only, decode fix must, parallel tracks, C++20+.txn, brand Transcender, session-00 skip, Hinglish chat / English docs)

---

## 1. What Changes vs Drafts (all 3 drafts merged, nothing dropped — user's correction #2)

| Area | Draft said | Re-context proves | Delta (action) |
|------|------------|-------------------|----------------|
| **Kernel LOC baseline** | 9,862 | **6,199** measured (`A2` wc -l 18 files). Duplicate `math_avx2_tensor.cpp` 613L inflates to 6,812 if counted, unique ~5,586. | Update Phase 23 target: need +18.8K not +15K to reach 25K. Baseline commit to `research/claim_ledger.md` as measured 6,199 (not 9,862). |
| **Format counts** | README 15 singles + 8 twi + 2 four | Registry `format_registry.cpp:20-96` = **19 singles + 0 twi + 14 four** (`A5` + `A1`) | README:147 + :5061 must be fixed in Phase 10 W20 and again in Phase 24 docs. Counts are `FORMAT_COUNT=105` law intact. |
| **Build targets** | 90+ targets, README 82 (25 libs) | CMake enumerates **112** (26 libs + 20 exes + 10 benches + 56 tests) (`A5`) | Docs Phase 24 to auto-generate from `CMakeLists.txt` (`rg add_library`). |
| **Adapters orphan** | 33 files orphan | Confirmed **ORPHAN** `src/adapters/` not in root build (`A5/A6` CMake), but `src/qwen35_tokenizer.cpp` + `src/k3_tokenizer.cpp` ARE in main build `CMakeLists.txt:131-132` — hybrid state | Decision: Phase 5 S5 maps orphans: `src/adapters/` → wire as `quant_adapters` (recommended for L070/L071 loaders) vs archive. Need owner gate. Keep `qwen35/k3` tokenizers in main build until rename (R8). |
| **Paged KV** | segfault open (S9-S12) | `A3` pinpoints `paged_kv_4m_test.cpp:80` clear→load→get_range + debug bleed `kv_cache.cpp:553` | W13 stays Phase 9/11, evidence path locked. |
| **ste_quantizer bug** | static codebook | Confirmed `40-48` + `51-55` first-tensor forever + thread race (`A1/A4` + `10-code-review-findings.md:3`) | Phase 6 P0, per-tensor fix (option A) recommended, then per-block (B) later in cender rewrite. |
| **GPU_VULKAN gap** | factory gap `backend.cpp:961` | Confirmed `A5` factory missing case, `gpu_compute_vulkan.cpp:986` GEMM passthrough CPU — double gap | Split W14 (factory) + W15 (GEMM) in Phase 10; both block Vulkan bar. |
| **Duplicate math** | not flagged | `math_avx2_tensor.cpp` identical to `math_avx2.cpp` latent LNK2005 (`A2`) | D1 immediate delete in Phase 5. |
| **Transformer dead tail** | 894L file | `A3` identifies 373L dead tail `transformer.cpp:515-892` 5 classes duplicate `kda/mla` | D3 split/delete in Phase 5. |
| **Invisible tests** | 52/56 green | `A1` test_fuzz gap skip `v==19`, `A2` test_math/kernel shell 3/30 APIs, `A3` missing sampler/transformer/kv_cache_quant4, `A4` missing autograd/optimizer, `A6` weak flywheel — total **7 missing test files** | Phase 11 + backlog waves to add; until then ledger stays RED BY DESIGN for 3. |
| **Bar** | unspecified | Gauntlet Loop requires Named+Fetchable+Comparable. Bars proposed: llama.cpp + GGUF/GPTQ + DeepSeek-V2/DeltaNet. User demanded parallel. | **Bar freeze** `bar/bar.sha256` locked 2026-09-01 with 3 bars combined: A llama.cpp `8887a48f`, B GGUF family, C DeepSeek-V2+DeltaNet. Re-fetch = new run. |

---

## 2. Re-Context Validated Problem Index

**P1-P73 (35 engineering-fixable, 15 strategy-doc):**
- Engineering-fixable now evidenced: P2/P3/P5 (data-gen), P10 bench harness, P12-P16 guardrails, P23 hot-swap, P35 tiled GEMM, P36 quantized KV, P31 macOS CI, P32 Vulkan, P38 error handling all map to W1-W28 above.
- Strategy-doc P59-P73 → `docs/STRATEGY.md` in Phase 18, not code.

**L001-L097 (9 waves):**
- L001-L007 Wave1/2 already validated via `research/workbench.md` DONE, but A5 proves gitignore still stale `.research/` + `dist` dup, Dockerfile `|| true` still present, bench enc/dec now split but decode speed pending — so Wave1 needs re-verify in Phase 12.
- L010-L017 Wave2: Q3_G fixed, QUAD/TWI asserts done (per workbench), but A1 proves `test_quant_mix` still broken TWI fixture — Wave2 re-verify.
- L020-L097 map cleanly onto W list; no orphan backlog item.

**W1-W24 from audit + new:**
- Original 23 wounds → `12-file-inventory.md` §4.3 W1-W28 (adds W22 dead MLA flags, W23 multimodal constants, W24 adapters orphan, W25 seed-42, W26 registry mapping, W27 affine mismatch, W28 BPW 31) — all evidenced file:line.

---

## 3. Execution Map — 25 Phases Frozen (with verified entry criteria)

| Phase | Name | Entry gate | Core work | Exit proof |
|-------|------|-----------|-----------|------------|
| **0** | Full Re-Context | — | 6 agents read-only → 12/13 | This doc + 12 + 6 reports |
| **1** | Re-Plan Synthesis | 12+13 | Merge 6 reports + 3 drafts → single backlog, resolve conflicts (second-opinion) | Consolidated backlog accepted (owner review) |
| **2** | Brand Rename | 1 done | InNova→Transcender root rename, version one-truth, header banners | `rg -i innova` 0 + build green |
| **3** | Provenance Cleanup | 2 green | File renames R1-11 + API de-brand + citation headers | G15 grep 0 + 52/56 baseline |
| **4** | Directory Re-Arrangement | 3 green | `git mv` to `src/{core,math,kernel,codec,model,inference,...}` mirror includes, CMake lists | mirror + build green (atomic per sub-batch) |
| **5** | Orphan/Dead/Bloat | 4 green | D1-D9, W21, W24, god-file splits, reconcile servers/trainers | git clean + build green |
| **6** | ste_quantizer Fix | 5 green | W1 per-tensor codebook, cover Q6/Q12/Q16 or error, test `test_ste_codebook.cpp` | test green |
| **7** | Decode Slowness | 6 green | W2 vectorize LUT, SIMD bit-unpack, fuse Q24/Q16/Q8_G, FastBitReader all formats | decode ≥2× faster table `research/claim_ledger.md` no PSNR regression |
| **8** | Wounds B1 (Training) | 7 green | W3-W5 (MoE grad, PPO/DPO, grad accum, R-Drop, prefetcher) | grad checks + tests |
| **9** | Wounds B2 (Model/Inf) | 8 green | W6-W13 (MLA cache, MTP, batching, spec, YARN, load, multimodal, paged_kv) | tests+evidence |
| **10** | Wounds B3 (Platform) | 9 green | W14-W20 (Vulkan, AGI harness, dead statics, seed-42, version, CI, BPW owner-gated) | tests+owner BPW decision logged |
| **11** | Test Repair | 10 green | `test_fuzz_codec`, `test_quant_mix` MXQ redesign, `paged_kv_4m`, missing 7 files | 56/56 green (owner-gated) |
| **12** | Backlog Waves 1-2 | 11 green | L001-L007 + L010-L017 | TRANSCRIPT PART-W + regression green |
| **13** | Waves 3-4 (Truth) | 12 green | L020-L037 real-model eval (≥3 HF), PPL harness, GGUF head-to-head, GRP 2×, fuzz 10k | `docs/REAL_EVAL.md` + reproduc scripts |
| **14** | Wave 5 (Speed) | 13 green | L040-L046 profile→SIMD→parallel encode → re-bench | speed table before/after |
| **15** | Wave 6 (Model feats) | 14 green | L050-L061 MLA/MTP/FP8/GatedDelta/KDA/hybrid/YARN/MoE etc. each impl or retired w/ ledger | per-item evidence, no shells |
| **16** | Wave 7 (Ecosystem) | 15 green | L070-L079 LLaMA loader, MoE loader, SentencePiece, trainer unify, server harden, OpenAI endpoints, macOS CI | contract suite + CI Win+Lin+mac |
| **17** | Waves 8-9 (GPU/Final) | 16 green | L080-L097 CUDA/Metal/Vulkan real kernels + persona sweep + perf/quality/security | wave-9 audit pass |
| **18** | P1-P73 Eng Subset | 17 green | Categories 1-7 per master plan §18, 8-10 → `docs/STRATEGY.md` | per-problem test/doc |
| **19** | 5-Pillar PoC | 18 green | 5 CLI demos `cender/tools/` RLM Router etc. | 5 demo runs pass |
| **20** | cender Scaffold + .txn IR | 19 green | Layout + lexer/parser/validator/compiler | round-trip tests + `IR_SPEC.md` |
| **21** | cender Modules | 20 green | AdditiveDelta, LatentKV, TranscenderMoE, PageStore, Trainer | unit+parity suites |
| **22** | cender Trainer+Bridge | 21 green | .quant bridge bidirectional, tiny model end-to-end | cross-engine round-trip |
| **23** | Kernels 25K+ | 22 green | K1-K6 in order, 6,199→25K (+18.8K) real | `wc -l` ≥25K + bench |
| **24** | Docs/PDFs | 23 green | README one-truth, ARCH/BUILD/USAGE, RESEARCH, whitepaper 13 ch + arxiv | G12 no stale numbers, PDFs current |
| **25** | Final Gates → v0.2 PROVEN | 24 green | 17/17 gates G1-G17 | tag v0.2 PROVEN |

**Hard dependency:** 0-4 before parallel tracks diverge (rename collisions). Thereafter Track A (InNova repair A1-A6 → Phases 5-17) + Track B (cender B1-B5 → Phases 19-23) + Docs C (Phase 24) as independent sub-agents with verifier per PR-sized change — orchestrator merges, owns shared files (CMake/tests).

---

## 4. Risks & Mitigations (from re-context)

1. **BPW ironclad (31 violations, `test_format_audit:79L` RED BY DESIGN).** Mitigation: owner-gated W28 in Phase 10 — fit encoders to claim OR rename doc strings to actual (e.g., `Q8_G ≡ Q_G_8.5`). Until decided, 3 tests stay red; do not "fix tests to pass" (Rule 14).
2. **Duplicate math** latent LNK2005. Mitigation: D1 delete in Phase 5 before any AVX512 work.
3. **Vulkan double gap.** Mitigation: W14+W15 both P0 in Phase 10; bar A (llama.cpp) comparison must note Vulkan GEMM is CPU until SPIR-V exists.
4. **Test debt (7 missing files).** Mitigation: Phase 11 adds them; bench enc/dec split already done but decode speed pending Phase 7/14.
5. **Adapters orphan drift.** Mitigation: Phase 5 S5 owner decision wire vs archive; Phase 16 L070 needs it for LLaMA loader.

---

## 5. Next Step (owner gate)

- Owner reviews this `13-replan-20260901.md` + `12-file-inventory.md` + `bar/bar.sha256`.
- Upon ACK, Phase 1 is considered DONE (synthesis) and Phase 2 (Brand Rename) starts with mechanical `rg` sweep + `git mv` + version one-truth.

---

## 6. Provenance

- All numbers measured, not guessed: `wc -l` kernels 6,199, CMake 112 targets, FORMAT_COUNT 105, 31 BPW violations via `test_format_audit.cpp`, bar hashes via `git rev-parse` + `sha256sum`.
- Honest verdicts only; every file verdict has `file:line` evidence in its agent report.
- Chat Hinglish / commits English per `02-iron-rules.md` Rule? + `01-user-profile.md`.
- Gauntlet Loop bar frozen `bar/bar.sha256` — re-fetch mid-run = new run.

