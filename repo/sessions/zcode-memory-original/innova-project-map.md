---
name: innova-project-map
description: Full absorbed map of InNova repo — Aug-26 full audit results, rename-halt state, BPW decision pending, live phase status
metadata:
  node_type: memory
  type: project
  originSessionId: sess_0dc0957a-5b90-4453-af4d-26a49d448f6a
---

InNova = pure C++20 zero-dependency AI engine, own `.quant` format (~82K LOC src; v3 spec: FORMAT_COUNT=105, enum hole at 19). Truth chain: TRANSCRIPT.md → `.research/workbench.md` → `.research/claim_ledger.md`; NEW authoritative audit = `.research/audit_full_20260825.md` (2026-08-26 full solo audit; sub-agents unusable this env).

**HALT STATE (top thing to know):** User ordered `_GRP`→`_G` rename over ALL files (Q3_GRP→Q3_G, Q3_K_M_GRP→Q3_K_M_G, Q_GRP_X.5→Q_G_X.5, MXQ_*_GRP→MXQ_*_G), then shouted STOP mid-flight ("Bas yaar, bas ab ruk jaa! STOP EVERYTHING") — user then asked "forget rename, what were we doing" and to see the audit report inline; nothing further authorized after STOP. **28/43 files renamed incl. include/quant/types.h enums + string names**, so the tree DOES NOT BUILD until remaining enum-name users are aligned (or reverted): PENDING = src/adapters/{include/adapters/adapter_core.h, src/adapter_core.cpp, tools/bench_industry_real.cpp, bench_quant_loss.cpp, probe_t4.cpp, quant_chat.cpp, quant_import.cpp}, tests/{test_format.cpp, test_format_audit.cpp, test_quant_mix.cpp}, tools/{generate_comparison_visuals.cpp, quantize.cpp}, .research/{claim_ledger.md, workbench.md}. NOTE: `.research/audit_full_20260825.md` was listed pending but a later on-disk read showed it already uses _G names — trust-audit-on-resume rather than assume. Everything UNCOMMITTED. Lowercase internal ids (grp16_fit_scale, kGrp16Size, format_is_grp) intentionally NOT part of the rule (family word GRP stays in prose/comments too). Related: [[bpw150-test-deletion-intentional]] — its dead CMake refs were also removed (fix B-1).

**BPW IRONCLAD VIOLATIONS = OPEN OWNER DECISION:** tests/test_format_audit.cpp (new probe) proves **31 formats store more bits than claimed**: Q2_GRP-family +0.625..+0.4375 (affine/grp16 budgets 2.625/3.5/4.5 etc.), Q6/Q6_K_G 6.5625 vs 6.0, Q8/Q8_K_G compound exactly 8.5 vs 8.0 claim (block_codec grp8_compound_fits enforces 8.5n), Q12/Q16/Q24 same +0.5, MXQ_{3.5..12.5}_G +0.125..+0.28 (budget guard computes scale bytes per-64, writer emits per-32). Consumer harm: select_best_format (format_registry.cpp:~329) gates on claimed bpw while storing actual. Probe exits 1 BY DESIGN until user picks: (a) fit encoders into claims, (b) make format_bpw/select report truth, (c) both. So test_fuzz_codec fails honestly ("payload exceeds claimed budget") — that's the contract working, not a regression.

**Fixed this session (all uncommitted):** 3 build-breakers (dead bpw_150 CMake entry; tensor.cpp→AutogradEngine LNK break via new layering hook quant::detail::autograd_unregister_hook in tensor.h/tensor.cpp/autograd_engine.cpp; orphan hybrid_expert.cpp added to quant_model) → tree went unbuildable→0-error build; codebook serialize/deserialize magic mismatch (QUA8 0x51554138 vs expected 0x4F494C38, QUA4 similar); QUANTReader pre-v3 relic `wire_fmt<38?:(Format)wire_fmt:Q32` coercion → FORMAT_COUNT boundary; PagedKVCacheBase::append current_pos advanced by 1 token regardless of block-size writes (kv_cache.cpp ~:384) → advance by tokens written; FastBitReader (word-buffered LSB-first, past-end-zeros semantics identical) swapped into 7 dequant paths (lattice, fixed_codebook, grp16, 6k, affine, grp12_compound, quad_mix + get_value signature) — bit-exact verified via tests+fuzz caps; 6 stale-test migrations (test_all FORMAT_COUNT 37→105 skip hole19; mix_components asserts TWI registry EMPTY by design; grp_proof enum ids 10-18→37-45 + explicit "Q4_G" lookup post-rename; mixed_precision requests GRP descriptor directly; paged_kv structural capacity assert vs TABLE_ENTRIES=4096²·bs (was silently bumped from 512-era 4M contract); fuzz caps table extended to all 104 formats + kFormats=104).

**Fresh ctest state before rename halt:** 52/55-class pass (95%). Remaining 3: test_fuzz_codec (correctly enforcing BPW contract), test_quant_mix (silent abort exit-3; TWI-era fixtures need MXQ redesign — Phase-3 scope), paged_kv_4m (SEGFAULT in far-page/disk-offload path, newly reachable after append fix — latent bug, needs dedicated debug).

**Bench tool is STALE post-v3 (evidence-integrity):** bench_format_comparison.cpp still benchmarks deleted TWI_MIX names and only 43 rows/dataset, never extended to 105. Fresh Debug run gave Q8_GRP gaussian 50.23 dB ≠ speed1-era CSV 58.88 ≠ direct codec probe 46.41@σ0.1 (compound taken, 272B, beats plain Q8 +5dB → codec itself healthy). ⇒ ALL CSV-derived claims (W2 beats GGUF Q8_0, W3, speed tables) need re-measurement after harness migration; ledger A-02 says NOT REPRODUCIBLE. Release-build speed numbers still unmeasured (Release run flaked once on Windows parallel links — use --parallel 3; rc.exe/LNK1327 & link 0xC0000142 at -j8 known).

**Shells/fakes re-confirmed alive at current lines (rebuild backlog, ledger A-03..A-13):** DPO train_step steps optimizer without ever producing grads (trainer_rl.cpp:342-394); PPO trains critic head only (:160-297); continuous-batching mask built then unused (inference_opt.cpp:333-334); spec-decode acceptance p_draft=1/vocab hardcoded (:240); SpeculativeDecoderV2 ghost decl (inference_opt.h:262); rewind_kv zero callers; MTP zero callers (defs model.cpp, wrapper trainer_core.cpp:742 unused); MLA discards cache ((void)cache mla_attention.h:48-49); YARN attn-factor discarded (transformer.cpp:138 unnamed, :166 mscale=1.0, :176 void); encode_image returns {30000} (multimodal.cpp:674-676); AGI self_play = round-robin 32 hardcoded prompts (agi_flywheel.cpp:808-840); GPU_VULKAN selectable (backend.cpp:1389) but factory has no case (:963-988) → silent fallback; moe_variants.cpp (1730L) ZERO grad/backward refs (routing breaks autograd); init_prefetcher decl-only moe_model.h:65; MoE aux loss fed dummy tensors (moe_trainer.cpp:281-295); quant_engines_core.cpp = dead file (static fns, zero callers); whole src/adapters/ tree orphaned from root CMake (open decision B-4); loss-scale "~128x shrink" wound NOT reproduced (math correct now); seed-42 RNGs at ~25 sites remain.

**TRANSCRIPT contradictions documented:** PART-AD claims "zero BPW violations found" while its own tables list Q8_GRP=8.50; PART-AD "Q3_GRP BROKEN top bug" + "next milestone L010" stale (fixed long ago, grp_proof 29.2dB ✓); workbench old Blockers text understated failures.

Positive/verified real: block_codec engineering core (f32↔f16 RNE, true-MSE golden search w/ clipping, closed-form Q1 s*=A/K, Lloyd-Max + N(0,1)-quantile fixed codebooks, signed-min affine, companded-Q8 LUT); FormatRegistry startup MSE measured on 3×16384 samples; allocate_mix_blocks greedy benefit-per-byte under hard ACTUAL-bytes budget; SHA-256 correct + hardened reader bounds; GLE hash-chain telemetry; FP8 E4M3/E5M2 kernels; AVX2 gather GEMMs; VectorQuantizer EMA/RVQ; audit trail in .research/claims/*.md updated.

Iron rules BIND (unchanged): BPW ironclad, Adafactor-only, STE mandatory, no stubs/TODOs, numbers measured not hand-copied, Hinglish chat + English commits, PERSONA systems permanently banned (D-8), llama.cpp black-box measurement bar only. Verify "DONE" only with fresh evidence. Trust .research/* over README claims. Related: [[bpw150-test-deletion-intentional]], [[user-hinglish-casual]], [[innova-workflow-constraints]].
